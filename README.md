Exemplo de criação local de repositório para
treinamento no git e github

Exemplo de arquivo

git init feito
git add dos arqivos anteriores feito
git status

git commit -m "primeira versão"

criando tags
git tag -a v0.1.0 -m "Mensagem"